# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RssReader' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Alamofire', '~> 4.5'
  pod 'HexColors'
  

  # Pods for RssReader

  target 'RssReaderRssReaders' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RssReaderUIRssReaders' do
    inherit! :search_paths
    # Pods for testing
  end

end
